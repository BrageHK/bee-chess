name: CI / Lab

on:
  push:
    branches: [main]
    paths:
      - 'lab/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/ci-lab.yml'
  pull_request:
    paths:
      - 'lab/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/ci-lab.yml'

concurrency:
  group: ci-lab-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lab:
    name: Lab
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: lab
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          # lab/ is a member of the root Cargo workspace (see /Cargo.toml).
          workspaces: .
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test
