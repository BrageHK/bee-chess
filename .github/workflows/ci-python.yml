name: CI / Python

on:
  push:
    branches: [main]
    paths:
      - 'training/**'
      - '.github/workflows/ci-python.yml'
  pull_request:
    paths:
      - 'training/**'
      - '.github/workflows/ci-python.yml'

concurrency:
  group: ci-python-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  python:
    name: Python
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: training
    env:
      # mamba-mcts-native (PyO3) doesn't officially support Python 3.14 yet
      # -- see the comment in training/pyproject.toml next to that dependency.
      PYO3_USE_ABI3_FORWARD_COMPATIBILITY: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
      - name: Sync
        run: uv sync
      - name: Lint
        run: uv run ruff check .
      - name: Test
        run: uv run pytest
