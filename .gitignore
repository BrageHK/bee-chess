# Rust
# One shared target/ at the repo root, not /engine/target/, since
# engine/ and lab/ are both members of the root Cargo workspace.
/target/
# mamba/mamba_mcts_native and mamba/mamba_mcts_batched_uci are deliberately
# their own workspaces (see their Cargo.toml files), so they build into
# their own target/ instead of the shared one above.
/mamba/mamba_mcts_native/target/
/mamba/mamba_mcts_batched_uci/target/
/mamba/mamba_mcts_batched_uci/.venv/

# Bee Lab
# Persisted finished-game history (#123) -- runtime data, not source.
/data/lab/

# bee-games / game-catalog
# The local SQLite catalog `bee-games` syncs into by default -- runtime
# data, not source.
/data/games/

# Python
/training/.venv/
/training/.pytest_cache/
/training/.ruff_cache/
/training/.cache/
/training/data/
/training/books/
/training/checkpoints/
/training/logs/
/bridge/.venv/
/bridge/.pytest_cache/
/bridge/.ruff_cache/
__pycache__/
*.pyc

# Node
node_modules/
dist/
dist-ssr/

# Editor / OS
.DS_Store
.vscode/
*.code-workspace
.idea/
